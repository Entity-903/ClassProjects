@model Game

@if (Game == null)
{
				<h2>No Movie Provided</h2>
}
else
{
				<h3>@Game.Title</h3>
				<h3>@Game.Platform</h3>
				<h3>@Game.Genre</h3>
				<h3>@Game.AgeRating</h3>
				<h3>@Game.Year</h3>
				<img src="@Game.Image" width="200" />
				<input type="hidden" name="id" value="@Model.Id" />


@*				<a asp-controller="Game" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-secondary mx-2">
						<i class="bi bi-wrench"></i> Edit
				</a>
				<a asp-controller="Game" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger mx-2">
						<i class="bi bi-twitter-x"></i> Shadow Realm
				</a>*@
}

@if (Game.LoanDate.HasValue)
{
	Game.LaonDate.Value.ToShortDateString();
}
